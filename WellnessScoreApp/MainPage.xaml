<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WellnessScoreApp.MainPage">

    <ScrollView
        x:Name="MainScrollView">
        <Grid
            RowDefinitions="Auto, Auto, Auto, Auto, Auto"
            RowSpacing="15">
            <Grid
                Grid.Row="0"
                ColumnDefinitions="*,*"
                ColumnSpacing="20"
                Padding="10">
                <Frame
                    x:Name="FrameMale"
                    Grid.Column="0"
                    BorderColor="Gray"
                    CornerRadius="15"
                    HasShadow="True">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnMaleTapped" />
                    </Frame.GestureRecognizers>
                    <VerticalStackLayout>
                        <Image
                            Source="male"
                            HeightRequest="30"
                            />
                        <Label
                            Text="Male"
                            FontSize="Body"
                            HorizontalOptions="Center"
                            />
                    </VerticalStackLayout>
                </Frame>
                <Frame
                    x:Name="FrameFemale"
                    Grid.Column="1"
                    BorderColor="Gray"
                    CornerRadius="15"
                    HasShadow="True">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnFemaleTapped" />
                    </Frame.GestureRecognizers>
                    <VerticalStackLayout>
                        <Image
                            Source="female"
                            HeightRequest="30"
                            />
                        <Label
                            Text="Female"
                            FontSize="Body"
                            HorizontalOptions="Center"
                            />
                    </VerticalStackLayout>
                </Frame>
            </Grid>
            <VerticalStackLayout
                Grid.Row="1"
                Spacing="10">
                <Label
                    Text="Sleep (hours)"
                    HorizontalOptions="Center"
                    FontSize="Body"/>
                <Slider
                    x:Name="SliderSleep"
                    Minimum="0"
                    Maximum="12"
                    Value="7"
                    BackgroundColor="White"/>
                <Label
                    Text="{Binding Source={x:Reference SliderSleep}, Path=Value, StringFormat='{0:F1} hrs'}"
                    HorizontalOptions="Center"
                    FontSize="Body"
                    FontAttributes="Bold"
                    x:Name="LblSleep"/>
            </VerticalStackLayout>
            <VerticalStackLayout
                Grid.Row="2"
                Spacing="10">
                <Label
                    Text="Stress Level"
                    HorizontalOptions="Center"
                    FontSize="Body"/>
                <Slider
                    x:Name="SliderStress"
                    Minimum="0"
                    Maximum="10"
                    Value="4"
                    BackgroundColor="White"/>
                <Label
                    Text="0 = very calm, 10 = very stressed"
                    HorizontalOptions="Center"
                    FontSize="Body"/>
                <Label
                    Text="{Binding Source={x:Reference SliderStress}, Path=Value, StringFormat='Level {0:F0}'}"
                    HorizontalOptions="Center"
                    FontSize="Body"
                    FontAttributes="Bold"
                    x:Name="LblStress"/>
            </VerticalStackLayout>
            <VerticalStackLayout
                Grid.Row="3"
                Spacing="10">
                <Label
                    Text="Activity Minutes (today)"
                    HorizontalOptions="Center"
                    FontSize="Body"/>
                <Slider
                    x:Name="SliderActivity"
                    Minimum="0"
                    Maximum="120"
                    Value="30"
                    BackgroundColor="White"/>
                <Label
                    Text="Any exercise, walking, or physical activity counts."
                    HorizontalOptions="Center"
                    FontSize="Body"
                    />
                <Label
                    Text="{Binding Source={x:Reference SliderActivity}, Path=Value, StringFormat='{0:F0} minutes'}"
                    HorizontalOptions="Center"
                    FontSize="Body"
                    FontAttributes="Bold"
                    x:Name="LblActivity"/>
            </VerticalStackLayout>
        </Grid>
    </ScrollView>

</ContentPage>
